import React from 'react';
import { connect } from 'react-redux';
import {editTodo} from '../actions'


let EditTodo = ({id, dispatch}) => {
  return (
      <div>

      </div>
  )
}

EditTodo = connect() (EditTodo)

export default EditTodo